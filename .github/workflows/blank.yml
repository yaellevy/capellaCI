name: capellaCI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v2

    - name: Check Python version
      run: python -V

    - name: Test with main
      run:  run main.py
